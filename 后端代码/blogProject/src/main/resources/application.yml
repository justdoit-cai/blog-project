spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/db_blog?rewriteBatchedStatements=true&useSSL=false&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root
  jackson:
    date-format: yyyy-MM-dd HH:mm
  redis:
    host: localhost
    port: 6379
